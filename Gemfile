source 'https://rubygems.org'

group :development do
  gem 'vagrant', github: 'mitchellh/vagrant'
end
group :plugins do
  gem "vagrant-itamae-local", path: "."
end
# Specify your gem's dependencies in vagrant-itamae-local.gemspec
gemspec
