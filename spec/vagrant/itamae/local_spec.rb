require "spec_helper"

describe Vagrant::Itamae::Local do
  it "has a version number" do
    expect(Vagrant::Itamae::Local::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
